# cf-slack-support

Facade for the composable support monorepo. Prefer installing optional features as peers and registering them via `features: […]`.

See the [repository root README](../../README.md) for the full package map.
