{
  "name": "@cf-slack-support/integration-tests",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test:workers": "vitest run --config vitest.workers.config.ts",
    "test:coverage": "vitest run --config vitest.workers.config.ts --coverage"
  },
  "dependencies": {
    "cf-slack-support": "0.0.1",
    "hono": "4.13.1"
  }
}
